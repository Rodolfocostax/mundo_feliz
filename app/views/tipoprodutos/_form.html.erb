<%= form_with(model: tipoproduto) do |form| %>
  <% if tipoproduto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipoproduto.errors.count, "error") %> prohibited this tipoproduto from being saved:</h2>

      <ul>
        <% tipoproduto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
